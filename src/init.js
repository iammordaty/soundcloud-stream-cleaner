/*global StreamCleaner*/

// TODO: sc.resource, sc.stream, sc.hero, ..., ?
// TODO: może zamiast wywołania, tutaja powinno znaleźć się ciało StreamCleanera?
// TODO: strona artysty

(function () {
    const cleaner = Object.create(StreamCleaner);

    cleaner.init();
}());
